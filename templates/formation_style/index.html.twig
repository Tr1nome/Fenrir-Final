{% extends 'base.html.twig' %}

{% block title %}FormationStyle index{% endblock %}

{% block body %}
    <h1>FormationStyle index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for formation_style in formation_styles %}
            <tr>
                <td>{{ formation_style.id }}</td>
                <td>{{ formation_style.title }}</td>
                <td>
                    <a href="{{ path('formation_style_show', {'id': formation_style.id}) }}">show</a>
                    <a href="{{ path('formation_style_edit', {'id': formation_style.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('formation_style_new') }}">Create new</a>
{% endblock %}
